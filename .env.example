# FanCompass AI Environment Configuration Example
# Duplicate this file as '.env' inside the /backend directory for local development

PORT=8080
GEMINI_API_KEY="your_api_key"

# Firebase Service Account JSON (stringified). Keep blank to use local In-Memory database.
FIREBASE_SERVICE_ACCOUNT=
